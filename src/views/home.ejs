<!DOCTYPE html>
<html lang="pt-br" id="html" data-bs-theme=white>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>My Movie List</title>
    
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/c147ae9e45.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" integrity="sha384-wvfXpqpZZVQGK6TAh5PVlGOfQNHSoD2xbE+QkPxCAFlNEevoEH3Sl0sibVcOQVnN" crossorigin="anonymous">
</head>
    <body>

    <nav class="navbar navbar-expand-lg bg-body-tertiary shadow">

      <div class="container-fluid d-flex justify-content-around">

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <% try { %>
          <% usuario[0].Usuario %>
          <div class="collapse navbar-collapse">
            <a class="navbar-brand" href="/<%= usuario[0].Usuario %>">My Movie List</a>
          </div>
          <a class="navbar-brand" href="/perfil/<%= usuario[0].Usuario %>"><%= usuario[0].Usuario %></a>
          <a class="navbar-brand" href="/filmes/<%= usuario[0].Usuario %>">Filmes</a>
          <a class="navbar-brand" href="/series/<%= usuario[0].Usuario %>">Series</a>
          <span class="material-symbols-outlined" onclick="mudarDarkMode()" id="icone">
            dark_mode
          </span>
        <% } catch (error) { %>
          <div class="collapse navbar-collapse">
            <a class="navbar-brand" href="/" id="tituloNav" >My Movie List</a>
          </div>
          <a class="navbar-brand" href="/login">Login</a>
          <a class="navbar-brand" href="/cadastrar">Registrar</a>
          <span class="material-symbols-outlined" onclick="mudarDarkMode()" id="icone">
            dark_mode
          </span>
        <% } %>
          
      </div>

    </nav>

    <main>

      <div class="container p-3 d-flex align-items-end mb-5 shadow-lg" id="containerPesquisa">

        <% if(usuario != '') { %>
          <form action="/buscaFilmes/<%= usuario[0].Usuario %>" method="post" id="formPesquisa">
            <input class="form-control p-2" name="buscaFilme" id="buscaFilme" placeholder="Pesquise um filme">
          </form>
        <% } %>
        
        <i class="fa-solid fa-magnifying-glass" id="imgLupa"></i>

      </div>

      <div class="container border border-black p-3 d-flex justify-content-center align-items-center mb-5">
        <div class="container border border-black row p-3 d-flex justify-content-around align-items-center">
          <h1>Lançamentos</h1>
          <div id="lancamentos" class="overflow-x-auto overflow-y-hidden d-flex flex-row">
            <% try { %>
              <% for(let i = 0; i < filmesMaisRecentes.length; i++) { %>
                  
                  <div class="infoFilme d-flex flex-column justify-content-between align-items-center">

                    <a href="/filmeSelec/<%= filmesMaisRecentes[i].IDFilme %>/<%= usuario[0].Usuario %>">
                      <img  class="imagemFilme" src="/img/<%= filmesMaisRecentes[i].Imagem %>" alt="Imagem não encontrada">
                    </a>
                    
                    <div class="notaFilme">
                      <%= filmesMaisRecentes[i].NotaGeral.toFixed(1) %>
                    </div>

                  </div>

              <% } %>
            <% } catch (error) { %>
              <% console.log(error) %>
              <p>Lista Vazia!</p>
            <% } %>
          </div>
        </div>
      </div>

      <div class="container border border-black p-3 d-flex justify-content-center align-items-center mb-5">
        <div class="container border border-black row p-3 d-flex justify-content-around align-items-center">
          <h1>Melhor Avaliados</h1>
          <div id="melhorAvaliados" class="overflow-x-auto overflow-y-hidden d-flex flex-row">
            <% try { %>
              <% for(let i = 0; i < filmesMelhorAvaliados.length; i++) { %>
                  
                  <div class="infoFilme d-flex flex-column justify-content-between align-items-center">

                    <a href="/filmeSelec/<%= filmesMelhorAvaliados[i].IDFilme %>/<%= usuario[0].Usuario %>">
                      <img  class="imagemFilme" src="/img/<%= filmesMelhorAvaliados[i].Imagem %>" alt="Imagem não encontrada">
                    </a>
                    
                    <div class="notaFilme">
                      <%= filmesMelhorAvaliados[i].NotaGeral.toFixed(1) %>
                    </div>

                  </div>

              <% } %>
            <% } catch (error) { %>
              <% console.log(error) %>
              <p>Lista Vazia!</p>
            <% } %>
          </div>
        </div>
      </div>

    </main>
    <footer class="border border-black">
      <div class="container border border-black">

      </div>
    </footer>
    <script src="/js/script.js"></script>
    <script>
      adicionarMode();
    </script>
    
</body>
</html>